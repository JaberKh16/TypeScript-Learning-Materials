// defining a enum => default setup
enum seatAllocationChoice {
  Aisle,
  Middle,
  Window,
}

enum colorChoice {
  Red = 11,
  Blue = 12,
  White, // 13
  Black, // 14
}

const firstSeat = seatAllocationChoice.Aisle;
